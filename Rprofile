# Jeffrey Pullin's profile 
# Last modified 6/7/2020

# Set the default cran mirror.
options(repos = structure(c(CRAN = "https://cloud.r-project.org/")))

# Overwrite the q() function so that it does not save by default
#utils::assignInNamespace(
#  "q",
#  function(save = "no", status = 0, runLast = TRUE) {
#    .Internal(quit(save, status, runLast))
#  },
#  "base"
#)

#Sys.setenv("_R_CHECK_LENGTH_1_LOGIC2_" = TRUE)
#Sys.setenv("_R_CHECK_LENGTH_1_CONDITION_" = TRUE)

options(warnPartialMatchAttr = TRUE,
        warnPartialMatchDollar = TRUE,
	warnPartialMatchArgs = TRUE,
        warn = 1)

# Fancy quotes are annoying and lead to
# 'copy + paste' bugs / frustrations
options(useFancyQuotes = FALSE)

options(
  usethis.full_name = "Jeffrey Pullin",
  usethis.protocol  = "ssh",
  usethis.description = list(
    `Authors@R` = 'person("Jeffrey", "Pullin", email = "jeffrey.pullin@gmail.com", role = c("aut", "cre"),
    comment = c(ORCID = "0000-0003-3651-5471"))',
    Version = "0.0.0.9000"
  )
)
